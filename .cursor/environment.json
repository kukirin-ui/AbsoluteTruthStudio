{
  "name": "Absolute Truth Studio",
  "install": "npm ci && npx playwright install chromium",
  "terminals": [
    {
      "name": "dev",
      "command": "npm run dev",
      "description": "Vite dev server (TanStack Start) bound to 0.0.0.0:8080 — serves the live Absolute Truth Studio app. Logs and HMR are visible here."
    }
  ],
  "ports": [
    {
      "name": "app",
      "port": 8080
    }
  ]
}
